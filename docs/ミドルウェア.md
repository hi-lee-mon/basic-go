メリットは
- コードの再利用
- 責務分離。ビジネスロジックから共通処理を分離する

渡し方は3種類

1.全てのルートにミドルウェアを実装する
```go
gin.Default().use(M)
```

2.個別
複数渡すことも可能
```go
gin.Default().GET("",M1,M2,controller.Create)
```

3.ルートグループ単位
```go
gin.Default().Group("/auth",M,M2)
```