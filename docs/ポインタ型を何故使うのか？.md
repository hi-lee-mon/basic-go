nilを許容するために使う

例えば
```go
type Item struct {
	Name *string
}

var i = Item{}
```
とすることができる。

---

調べて見ると上記はおまけレベルで本当は共有とパフォーマンスのためにある。

ポインタ型にしないと構造体の持つ状態が消える。またDBからバカでかいデータを取得したときにポインタ型じゃないと毎回コピーされてしまう。
goを書く場合殆どの場合はポインタ型を使うことになりそう。